let a: number = 3;
let b: string = "Mehmet";
let c: boolean = true;
let d: any;
let e: number[] = [1, 2, 3];
let g: any[] = [1, "Mehmet", true];
let h: [string, number, boolean] = ["Mehmet", 1, true];

enum Payment {
	kredi,
	havale,
	eft,
}

export {};
